/// <summary>
/// カード(キャラクター)が持つ6属性。
/// 実際のカードデザイン（闇・火・光・水・地・風）に合わせてある。
/// 以前は仮の名称として Thunder(雷) を使っていたが、実カードには存在しないため
/// Dark(闇) に修正した。
/// </summary>
public enum ElementType
{
    Fire,    // 火
    Wind,    // 風
    Dark,    // 闇
    Water,   // 水
    Earth,   // 地
    Light    // 光
}
