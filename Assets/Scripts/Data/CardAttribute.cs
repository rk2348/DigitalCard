namespace Orisamo.Data
{
    /// <summary>
    /// カードが持つ6属性。
    /// 正式名称は企画側で確定次第、仮名(AttrA〜AttrF)を置き換える。
    /// enum名や値の並び順は変更しないこと（AttributeMatrixSOのインデックスと対応するため）。
    /// </summary>
    public enum CardAttribute
    {
        AttrA = 0,
        AttrB = 1,
        AttrC = 2,
        AttrD = 3,
        AttrE = 4,
        AttrF = 5
    }
}
